<!-- agents-sync 生成；请勿直接编辑 -->

# 全局 Hermes 指令

# 交互式自动化限时、清理与结果校验

## 适用范围

- 本规则适用于 GUI / 浏览器操作、交互式终端或 PTY、连续 `write_stdin`、登录与授权流程、轮询、等待后台会话，以及其他需要多次工具调用才能完成的交互链路。
- 普通非交互式构建、测试和明确的长期监控不强制使用下方 30 秒默认总时限，但仍必须设置符合任务预期的有限超时，并按约定提供进度更新。

## 开始前建立边界

- 启动命令前先通过 `--help`、文档或已知协议判断它是否会等待输入。能使用参数、配置文件、标准输入一次性传参或专用非交互接口时，优先采用非交互方式。
- 开始交互链路前，必须明确：成功证据、单步时限、整条链路总时限、最大重试次数，以及超时或中断时如何终止本次创建的会话或进程。
- 预计需要超过 3 轮人工式问答的终端命令，不使用逐轮工具调用驱动；改用有总超时的一次性非交互方案。无法安全改造时，停止并说明需要用户接管。

## 成功证据

- 执行点击、输入、发送、提交、保存、安装或生成等动作前，先明确本次动作可观察的成功信号；没有成功信号，不得声称动作已完成。
- 优先用最权威的只读来源验证结果：API / CLI 返回的对象或消息 ID、服务端记录、后台状态；没有专用接口时，再读取最新 UI 状态或截图。
- UI 中出现输入内容、按钮被按下、终端显示提示符或某次 `write_stdin` 返回，只能证明链路推进到该步骤，不能证明目标结果已经产生。
- 终端任务以进程在总时限内退出并返回预期退出码、且目标产物通过独立检查为成功；仍在等待下一次输入的进程不算完成。

## 飞书界面发送

- 需要通过已登录的飞书客户端发送消息时，默认只使用已验证路径：先对消息输入框执行 `set_value`，再点击当前界面中可见的发送按钮。
- 不默认使用 `type_text`、Enter、`⌘+Enter` 或坐标猜测组合发送；只有重新读取当前界面并确认默认路径不可用，且存在明确证据表明替代路径可靠时，才允许改用替代路径。
- 点击发送后立即通过飞书消息记录或 API 校验是否出现新的消息 ID；输入框清空或点击动作返回不等于发送成功。
- 默认路径一次未产生新消息 ID 时，不自动尝试多种发送方式；停止 UI 发送并请用户配合向机器人发送指定消息。用户确认已发送后，再从消息记录继续后续流程。

## 限时与重试

- 普通单步 UI 或交互响应默认不超过 15 秒；简单端到端交互链路从第一次操作到结果核验默认不超过 30 秒。
- 已知必须更久的任务，开始前必须说明预期时长并设置明确的总截止时间；“工具仍在运行”“还可能有下一个提示”不能作为无限续期理由。
- 工具不支持精确设置单步超时时，不连续发起相同动作或无边界等待；完成一次调用后立即读取事实状态并决定下一步。
- 未观察到成功证据时，明确记录为「未确认」或「未完成」。最多自动重试一次，且重试前必须重新读取最新状态并改变可能失败的操作路径。
- 第二次仍无成功证据时立即停止，报告卡在哪一层以及最后一个已确认事实，不继续循环操作。

## 会话与进程清理

- 创建交互式会话或后台进程后立即记录其 session ID 或精确 PID；不得仅靠进程名模糊清理。
- 达到单步或总时限、工具调用异常、用户中断或发来替代指令时，立即停止继续输入，终止本次创建且仍在等待的会话或进程，并用只读检查确认它已退出。
- 中断可能留下部分外部状态时，必须核验产物、配置、凭据或服务是否发生变化；未核验前不得声称“没有修改”或“已经完成”。
- 不将仍等待输入的 PTY、授权弹窗或后台任务留给后续回合隐式继续；若因权限限制无法清理，立即向用户报告准确的 session ID / PID 与残留状态。

## 分层定位

- 分开判断 Agent 工具调用、交互会话或目标应用、应用后台 / API、下游系统四层状态，不用某一层正常推断其他层正常。
- 诊断卡顿时，先判断工具调用是否返回、外部状态是否改变、后台进程是否仍在工作，再归因于权限、网络或目标应用。
- 用户中断或发来替代指令时，停止原有交互链并执行上述清理；不得在新指令下继续隐式等待旧操作。

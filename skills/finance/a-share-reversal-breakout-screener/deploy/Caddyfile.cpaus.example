a-share.192-255-128-222.sslip.io {
	encode gzip

	basicauth {
		friend <bcrypt-hash>
	}

	reverse_proxy 127.0.0.1:8765

	header {
		Strict-Transport-Security "max-age=31536000"
		X-Content-Type-Options nosniff
		Referrer-Policy no-referrer
	}
}
